# Backend (Flask)

## Prereqs
- Python 3.10 or 3.11 recommended (many ML deps do not support Python 3.13 yet).

## Run locally
```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Backend runs on `http://localhost:5000`.

