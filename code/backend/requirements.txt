# Target runtime: Python 3.14 (standard CPython).
# Flask manages compatible versions of its own supporting packages.
Flask==3.1.3
flask-cors==6.0.2
sentence-transformers==5.2.0
# Keep the existing Transformers 4 pipeline API for the current QA model.
transformers==4.57.6
torch==2.10.0
# Python 3.14 requires newer NumPy and pandas releases.
numpy==2.4.3
pandas==2.3.3
# Needed for pandas.read_excel(...) used by api_controller.py
openpyxl==3.1.5
